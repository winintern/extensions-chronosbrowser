{
  "manifest_version": 3,
  "name": "Погода",
  "version": "1.0",
  "description": "Плавающая кнопка с текущей погодой для выбранного города. Данные — Open-Meteo (бесплатно, без ключа API). Геолокация браузера не используется (в ChronosBrowser она по умолчанию запрещена из соображений приватности) — город вводится вручную один раз и запоминается.",
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["content.js"],
      "run_at": "document_idle"
    }
  ]
}
